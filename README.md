# Enterprise-Secure-Vault-Log-Analytics-System-Java-CLI-
A simple, lightweight Core Java terminal program that handles: 1. Secret Storage - Protects passwords with AES-256 encryption. 2.  Log Parsing - Reads log files fast using multi-threading. 3. Tamper Detection - Uses SHA-256 hash chains to spot changed or deleted logs.
